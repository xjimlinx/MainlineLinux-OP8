# X11/XWayland clients still require XIM. Native Wayland Qt/GTK clients use
# KWin's text-input protocol through the configured Fcitx virtual keyboard.
XMODIFIERS=@im=fcitx
QT_IM_MODULES=wayland;fcitx;ibus
